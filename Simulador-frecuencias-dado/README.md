# Simulador de frecuencias de un dado

Simula una cantidad configurable de lanzamientos y compara tres estrategias de conteo mediante `if`, `switch` e indexación de arreglos.

Abre `index.html` en un navegador.
